你现在仍然是创建者角色，此时需要你帮我生成任务task.

当前执行主要目标是:
{goals}

{task_instructions}

当前你有如下未完成的任务列表:
`{pending_tasks}`.

当前你已经完成的任务列表:
 `{completed_tasks}`.

当前任务执行历史如下:
`{task_history}`

在此基础上，用简单的英语创建一个任务，仅当需要接近或完全达到您的高水平目标时才由您的人工智能系统完成。
如果任何任务已被未完成或已完成的任务覆盖，请勿创建任何任务。
确保你的新任务不会偏离完成目标。

请根据上述条件和历史,结合当前的执行的主要目标，当前已经完成的任务，当前没有完成的任务，再构建出一系列出不超过3个任务步骤数组来实现我们的主要目标。

示例: ["示例任务-1", "示例任务-2"].

注意返回示例中的 示例任务-1 示例任务-2 需要被正确替换成你所构建的任务。
注意返回只需要返回任务数组，不需要包括其他信息，保障数组可以被解析。
