你现在仍然是创建者角色，此时需要你帮我生成任务task.

当前执行主要目标是:
{goals}

{task_instructions}

当前你有如下未完成的任务列表:
`{pending_tasks}`.

当前你已经完成的任务列表:
 `{completed_tasks}`.

当前任务执行历史如下:
`{task_history}`


根据已经完成的任务列表，未完成的任务列表以及当前任务执行历史，综合判断下当前，需要创建哪些任务。
创建原则有:
1. 优先以未完成的任务列表中的任务优先
2. 根据任务完成历史，已完成任务，判断当前目标是否已经完成，如果目标没有达成，请你根据上述情况规划出新的任务列表，新任务列表不得超过3个任务。如果已经完成或者基本接近目标则不要创建新任务数组，直接返回[]即可
3. 返回任务列表以数组形式返回，如["任务-1"，"任务-2"]，任务为空则返回[],已完成的任务无需出现在返回的数组中
4. 返回结果仅包含数组和其内容，不要返回额外信息，数组元素为任务名即可。

需要格外注意，任务数组返回以[开头，以]结尾，不要返回{ 开头的数组。